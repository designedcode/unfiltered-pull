import { site } from "./site";

export function instagramDmHref() {
  return site.instagramDm;
}

export const firstPullHint = `Send ${site.firstPullKeyword} plus your name, your question, and your preferred format (Video, Audio, Chat, or Voice notes).`;

export const servicesHint = `Send ${site.servicesKeyword} or the reading name, then the details listed on that service page.`;
